#include <iostream>
using namespace std;

int main() {
    int n, a, b;
    std::cin >> n >> a >> b ;

    int ans = 0;
    for (int i = 0; i <= a; i++) {
        for (int j = 0; j <= b; j++) {
        }
    }
    cout << ans << endl;

    return 0;
}
